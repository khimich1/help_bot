# Чеклист RAG: ЖК Шкиперский

> Спека: [`docs/specs/interior-studio-knowledge.md`](../specs/interior-studio-knowledge.md)  
> Критерий приёмки: **≥8/10** ответов с верным источником и цитатой ≤200 символов.

## Подготовка

```powershell
cd c:\help_agent
python -m interior_studio.db.init_db
python -m interior_studio.db.seed_projects --file data/initial_projects.txt

# Положите папку проекта в корень репо (не в git):
#   ЖК Шкиперский/
python -m interior_studio.knowledge.index_project `
  --project "ЖК Шкиперский" `
  --path "ЖК Шкиперский"
```

В `.env` нужен `OPENAI_API_KEY` (embeddings + агент).

## Эталонные вопросы

Запускай через CLI:

```powershell
python -m interior_studio.agent.cli --trace "ВОПРОС"
```

| # | Вопрос | Ожидаемый источник (тип) | OK | Фактический источник | Заметки |
|---|--------|---------------------------|----|-----------------------|---------|
| 1 | Какой стиль интерьера по Шкиперскому? | `brief` | | | |
| 2 | Кто клиенты по Шкиперскому, есть ли дети? | `brief` или `questionnaire` | | | |
| 3 | Кухня — остров с барными стульями по Шкиперскому? | `brief` или `questionnaire` | | | |
| 4 | Какой цвет дверей по Шкиперскому? | `brief` / `site_report` | | | |
| 5 | Какая плитка в мастер-с/у по Шкиперскому? | `site_report` / `brief` | | | |
| 6 | Перепланировка по Шкиперскому согласована? | `brief` / `site_report` | | | |
| 7 | Высота потолка в коридоре после кондиционеров по Шкиперскому? | `site_report` | | | |
| 8 | Какие обои заменили по Шкиперскому? | `site_report` | | | |
| 9 | Пожелания Вики по гардеробной по Шкиперскому? | `questionnaire` / `brief` | | | |
| 10 | Размер проёма в спальню по Шкиперскому? | `site_report` / `brief` | | | |

## Что считать «верным ответом»

- [ ] Агент **сначала** вызвал `search_project_knowledge` (видно в `--trace`)
- [ ] Ответ на русском, суть + **источник** (файл/тип документа)
- [ ] Цитата из документа ≤200 символов
- [ ] Нет выдуманных фактов при пустом RAG

## Итог

| Метрика | Значение |
|---------|----------|
| Пройдено (OK) | /10 |
| Критерий ≥8 | ☐ да / ☐ нет |
| Дата проверки | |
| Проверил | |
